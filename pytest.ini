[pytest]
markers =
    topdown: Top-down integration tests (using stubs for lower components)
    bottomup: Bottom-up integration tests (testing individual components)
    sandwich: Sandwich integration tests (hybrid approach with real components)
